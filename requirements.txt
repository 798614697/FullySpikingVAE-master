# Install torch/torchvision separately for your GPU/CUDA version.
# RTX 5060 example:
#   python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# pycuda is optional in this project. GPU training uses PyTorch CUDA, not pycuda.
tensorboard
pyyaml
tqdm
matplotlib
scipy
clean-fid
pandas
